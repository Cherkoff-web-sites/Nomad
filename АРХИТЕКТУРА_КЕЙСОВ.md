# 🏗️ Архитектура кейсов: Правильный компонентный подход

## ❌ Проблема (что НЕ делать)

```jsx
// ПЛОХО: компонент знает о конкретных страницах
<p className={`${location.pathname === '/case16' ? 'w-[54%]' : 'w-[48%]'}`}>
```

**Почему плохо:**
- Компонент привязан к конкретным URL
- При добавлении нового кейса нужно менять компонент
- Нарушает принцип переиспользования

---

## ✅ Решение: Использовать пропсы (props)

### 1. Компонент принимает настройки через пропсы

```jsx
// ХОРОШО: компонент получает настройки извне
const TitleSection = ({ titleWidth = 'w-[48%]', titleAlign = 'items-center' }) => {
  return <p className={`${titleWidth} flex ${titleAlign}`}>...</p>
}
```

### 2. Каждая страница кейса передает свои настройки

```jsx
// Case16Page.jsx
const caseData = {
  projectTitle: 'Branding',
  clientTitle: '«ХВОЯ. ЛЕСНОЕ БИСТРО»',
  titleWidth: 'w-[54%]',  // ← Кастомная настройка
  titleAlign: 'items-center',
  // ... остальные данные
}
```

---

## 📋 Как добавлять новые кейсы

### Вариант 1: Использовать существующие компоненты (рекомендуется)

Если новый кейс похож на существующие:

1. **Скопировать структуру** существующего кейса (например, `Case3Page.jsx`)
2. **Изменить данные** в `caseData` и других объектах
3. **Добавить кастомные настройки** через пропсы:
   ```jsx
   const caseData = {
     // ... стандартные поля
     titleWidth: 'w-[54%]',  // если нужно
     titleAlign: 'items-start',  // если нужно
   }
   ```

### Вариант 2: Создать кастомные компоненты

Если нужен уникальный блок, которого нет:

1. **Создать новый компонент** в `src/components/`:
   ```
   src/components/
   └── CustomBlock/
       └── CustomBlock.jsx
   ```

2. **Использовать в кейсе**:
   ```jsx
   import CustomBlock from '../../components/CustomBlock/CustomBlock';
   
   // В JSX:
   <CustomBlock data={customData} />
   ```

---

## 🎯 Структура страницы кейса

```jsx
const CaseXPage = () => {
  // 1. Данные для InfoSection
  const caseData = {
    projectTitle: '...',
    clientTitle: '...',
    cards: ['...'],
    description: '...',
    // Кастомные настройки (опционально):
    titleWidth: 'w-[54%]',
    titleAlign: 'items-start',
    result: [...],  // если нужен блок результатов
  };

  // 2. Данные для блоков контента
  const type1Data = { ... };
  const type2Data = { ... };

  return (
    <main>
      <HeaderBlack />
      <InfoSection caseData={caseData} />
      
      {/* Стандартные блоки */}
      <img src={image1} />
      <Type1 type1Data={type1Data} />
      <Col2Photo image1={image1} image2={image2} />
      
      {/* Кастомные блоки (если нужны) */}
      <CustomBlock data={customData} />
      
      <NextPrevCase />
      <Footer />
    </main>
  );
};
```

---

## 📦 Доступные компоненты

### Базовые блоки:
- `InfoSection` - заголовок, теги, описание, результаты
- `Type1`, `Type2`, `Type3` - текстовые блоки с изображениями
- `Col2Photo` - два изображения рядом
- `HalfText` - текст на половину ширины
- `ImageCustom` - кастомное изображение
- `VideoParallel` - видео блоки

### Специфичные:
- `TargetGrid` (только в Case1)
- `LongImgVideo` - длинное изображение/видео

---

## 🔧 Кастомизация через пропсы

### InfoSection (caseData):
```jsx
{
  projectTitle: '...',      // Заголовок проекта
  clientTitle: '...',       // Название клиента
  cards: ['...'],           // Теги
  description: '...',       // Описание
  result: [...],            // Результаты (опционально)
  titleWidth: 'w-[54%]',    // Ширина заголовка (опционально)
  titleAlign: 'items-start', // Выравнивание (опционально)
  nowrap: true,             // Запрет переноса (опционально)
}
```

---

## ✅ Чеклист для нового кейса

1. [ ] Создать папку `CaseXPage/` с `CaseXPage.jsx`
2. [ ] Добавить изображения в `CaseXPage/assets/`
3. [ ] Скопировать структуру похожего кейса
4. [ ] Обновить `caseData` с нужными настройками
5. [ ] Добавить роут в `Routes.jsx`
6. [ ] Добавить данные в `src/pages/utils/const.js`
7. [ ] Добавить данные в `CasesPage/.../SortByCondition.jsx`
8. [ ] Добавить фоновое изображение в `public/images/bg-X.jpg`
9. [ ] Добавить в `tailwind.config.js` (если нужно)

---

## 💡 Принципы компонентного подхода

1. **Компоненты переиспользуемые** - не знают о конкретных страницах
2. **Настройки через пропсы** - каждая страница передает свои данные
3. **Кастомные блоки** - создаются как отдельные компоненты
4. **Единая структура** - все кейсы используют одинаковые базовые компоненты

---

**Итог:** Используйте пропсы вместо проверки `location.pathname`. Это правильный компонентный подход! 🚀

